# Insightra 项目约定

用户已确认现有中性浅底、细分隔线、大画布、侧边详情的风格，以及三主题切换。

- 修改界面前阅读 `docs/UI-UX.md`；扩展模块前阅读 `docs/ARCHITECTURE.md`。
- 延续现有应用框架和设计变量；新页面复用共享组件。不要为单个模块另建一套主题或导航。
- `src/styles.css` 管理应用外壳与主题；`src/modules.css` 管理业务页面公共样式。
- `src/workspaces.css` 管理研究资源工作台、连续式概览和家庭空间编辑器；不要恢复五阶段大导航或等大概览卡片。
- `src/data-health.css` 管理结构化数据编辑器、真实产业资料视图与健康日志；产业视图只能读取共享数据集，不恢复手写示例公司。
- `src/atlas-drag.css` 管理全行业公司地图与提起式拖拽。公司地图默认保留全部公司，不能退回环节圆圈或一跳关系作为主视图。
- 真实数据、合成示例、未接入、错误、过期缓存必须明确区分。不得用随机值填补真实接口失败。
- 不读取、输出、提交 `.env` 中的凭证。配置 API 不返回原值；前端和 localStorage 不保存凭证。
- Tushare 为只读固定目标接口。不得为了连接成功关闭 TLS 校验或自动切换到 HTTP。
- 知识库文本按纯文本展示，不把导入内容作为 HTML 执行。真实向量化之前不得称合成坐标为语义结果。
- 研究布局与算法分离，聚类不等同于力导布局；保留跨视图选择和来源信息。
- 改动后运行 `npm run build`，接口改动运行 `npm run test:api`，交互改动运行 `npm run test:ui`。
- 使用 `npm run format` 统一前端排版。浏览器测试不得覆盖用户真实配置，测试文档必须清理。
- 本机运行阶段不接入真实交易执行；后续用户明确提出相关工作时再按当时授权范围实施。
- 金融看板必须遵守三层：数据管理中的数据源同步 → 小组件声明 schema/字段并绑定数据集 → 看板添加、编组与保存。不得在小组件或看板中直接调用 Tushare 或写死单独行情页。
- 行情日线必须展示交易日期和非实时属性；股票快照与指数序列不能仅按相似字段混用。同步数据保留来源口径，只读展示，可复制为标注本地编辑的数据。
- 加载需先保留最终容器几何尺寸；刷新保留旧内容，失败明确标注。使用 `LoadingSurface.svelte` 与 `market-board.css` 的骨架、内容过渡和减少动态效果规则。
- `src/dashboard-dense.css` 管理看板紧凑层级与主题云图。保持单行工具栏、相关信息紧密成组；组件设置始终可用，支持独立刷新周期与 full 整行尺寸。标题旁显示检查时间、绿色成功/橙色失败/刷新中状态，交易日期独立显示。
